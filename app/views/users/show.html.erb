<div class="text-center">
<h1>ユーザー名 : <%= @user.name %></h1>
<h1>メールアドレス : <%= @user.email %></h1>

<%= link_to "キッチンスキルマップ編集", edit_kitchen_path(@kitchen_skill_map, id: @user.id ) %>
<%= link_to "ホールスキルマップ編集", edit_hall_path(@hall_skill_map, id: @user.id ) %>
<%= link_to "マップ切り替え", change_map_hall_path(@hall_skill_map.id, id: @user.id), remote: true %>

<canvas id="myChart" width="140" height="50"></canvas> 
<script>
    console.log("labels", gon.kitchen_skill_labels)
    draw_graph(gon.kitchen_skill_labels, gon.kitchen_skill_values);</script>
</div>
